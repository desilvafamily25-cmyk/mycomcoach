// This route group layout is preserved for future use.
// Protected pages are served from app/ root and use components/AppShell.tsx directly.
export default function AppGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
